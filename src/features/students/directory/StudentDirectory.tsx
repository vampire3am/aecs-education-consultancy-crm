import{useEffect,useState}from"react";import{ChevronLeft,ChevronRight,Eye,Filter,MoreHorizontal,Search,Trash2,UserPlus}from"lucide-react";import{Link}from"react-router-dom";import{deleteStudent,listStudents}from"../student.service";import{statusLabel,type Student}from"../student.types";import{useAuth}from"../../auth/AuthProvider";
const countries=["UK","Australia","Canada","USA","Japan","Korea"],statuses=Object.entries(statusLabel),tests=["IELTS","PTE","Duolingo","None"];
export function StudentDirectory(){const{profile}=useAuth();const[rows,setRows]=useState<Student[]>([]);const[count,setCount]=useState(0);const[loading,setLoading]=useState(true);const[search,setSearch]=useState("");const[country,setCountry]=useState("");const[status,setStatus]=useState("");const[testType,setTestType]=useState("");const[page,setPage]=useState(1);const[pageSize,setPageSize]=useState(20);useEffect(()=>{const t=setTimeout(()=>{setLoading(true);listStudents({search,country,status,testType,page,pageSize}).then(r=>{setRows(r.data);setCount(r.count)}).finally(()=>setLoading(false))},250);return()=>clearTimeout(t)},[search,country,status,testType,page,pageSize]);async function remove(s:Student){if(!confirm(`Delete ${s.full_name}? This cannot be undone.`))return;await deleteStudent(s.id);setRows(rows.filter(x=>x.id!==s.id));setCount(count-1)}return <><section className="page-heading"><div><p className="eyebrow">Student directory</p><h2>Students</h2><p>{count.toLocaleString()} total students</p></div><Link className="primary-button link-button" to="/students/new"><UserPlus size={17}/>Add student</Link></section><article className="panel directory"><div className="directory-tools"><div className="search large"><Search size={17}/><input value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}} placeholder="Search name, student ID, WhatsApp or email"/></div><div className="filters"><Filter size={16}/><select value={country} onChange={e=>setCountry(e.target.value)}><option value="">All countries</option>{countries.map(x=><option key={x}>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option>{statuses.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select><select value={testType} onChange={e=>setTestType(e.target.value)}><option value="">All tests</option>{tests.map(x=><option key={x}>{x}</option>)}</select></div></div><div className="table-scroll"><table><thead><tr><th>Student ID</th><th>Name</th><th>WhatsApp</th><th>Country</th><th>Course</th><th>Status</th><th>Counsellor</th><th>Created</th><th/></tr></thead><tbody>{loading?Array.from({length:5}).map((_,i)=><tr key={i} className="skeleton-row"><td colSpan={9}><i/></td></tr>):rows.map(s=><tr key={s.id}><td><Link to={`/students/${s.id}`}>{s.student_code}</Link></td><td><strong>{s.full_name}</strong><small>{s.email}</small></td><td>{s.whatsapp}</td><td>{s.study_preferences?.preferred_country||"—"}</td><td>{s.study_preferences?.preferred_course||"—"}</td><td><span className={`status-badge ${s.status.toLowerCase()}`}>{statusLabel[s.status]}</span></td><td>{s.staff_profiles?.full_name||"Unassigned"}</td><td>{new Date(s.created_at).toLocaleDateString()}</td><td><div className="row-actions"><Link aria-label="View profile" to={`/students/${s.id}`}><Eye size={16}/></Link>{["ADMIN","DIRECTOR"].includes(profile?.role||"")&&<button onClick={()=>remove(s)} aria-label="Delete"><Trash2 size={15}/></button>}</div></td></tr>)}</tbody></table>{!loading&&!rows.length&&<div className="empty-table"><MoreHorizontal/><h3>No matching students</h3><p>Try changing your search or filters.</p></div>}</div><footer className="pagination"><span>Showing {count?((page-1)*pageSize)+1:0}–{Math.min(page*pageSize,count)} of {count}</span><div><select value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}><option>10</option><option>20</option><option>50</option></select><button disabled={page===1} onClick={()=>setPage(page-1)}><ChevronLeft size={16}/></button><span>Page {page}</span><button disabled={page*pageSize>=count} onClick={()=>setPage(page+1)}><ChevronRight size={16}/></button></div></footer></article></>}
